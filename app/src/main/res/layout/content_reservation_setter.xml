<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:showIn="@layout/activity_reservation_setter"
        tools:context=".ReservationSetter">

    <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/reservation_navigation_tabs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:menu="@menu/bottom_menu"
            app:itemTextAppearanceActive="@android:color/black"
            android:background="@android:color/white"

            app:itemTextAppearanceInactive="@android:color/darker_gray"
            app:itemIconTint="@android:color/holo_red_light" />

    <View
            android:layout_width="wrap_content"
            android:layout_height="6.0dp"
            android:background="#ffe4e4e4"
    />

    <FrameLayout
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

    </FrameLayout>

</LinearLayout>